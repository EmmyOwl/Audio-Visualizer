import { isSupported } from 'standardized-audio-context';
export { isSupported };
export declare const load: (url: string) => {
    analyze: (audioBuffer: AudioBuffer, offset?: number, duration?: number) => Promise<number>;
    guess: (audioBuffer: AudioBuffer, offset?: number, duration?: number) => Promise<{
        bpm: number;
        offset: number;
    }>;
};
//# sourceMappingURL=module.d.ts.map